#include <vector>
#include <chrono>

#include <transducers\transduce.hpp>
#include <transducers\mapping.hpp>

int main(int count, char** args)
{
    std::vector<char*> arguments(args, args + count);

    
}